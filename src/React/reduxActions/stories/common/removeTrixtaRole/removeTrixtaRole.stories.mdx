import { ArgsTable, Title, Description } from '@storybook/addon-docs';
import { Meta } from '@storybook/addon-docs';
import DocsGenerator from './docs-generator';
import { DEFAULT_REDUX_ACTIONS_PATH } from '../../../../../stories/constants/storybook';

<Meta
  title={`${DEFAULT_REDUX_ACTIONS_PATH}removeTrixtaRole`}
  parameters={{
    status: {
      type: 'released', // 'beta' | 'stable' | 'deprecated'
    },
  }}
/>

<Title>removeTrixtaRole</Title>
<Description>
  This will leave a role for a connected Trixta Space. Removing all connected
  actions and reactions for the role.
</Description>

# Examples

## Using a saga

```javascript
import { removeTrixtaRole } from '@trixtateam/trixta-js';
import { put } from 'redux-saga/effects';
yield put(removeTrixtaRole({name:'name of Trixta role'}))

```

## Using useDispatch hook

```javascript
import { removeTrixtaRole } from '@trixtateam/trixta-js';
import { useDispatch } from 'react-redux';
const dispatch = useDispatch();
yield put(removeTrixtaRole({name:'name of Trixta role'}))

```

<ArgsTable of={DocsGenerator} />
