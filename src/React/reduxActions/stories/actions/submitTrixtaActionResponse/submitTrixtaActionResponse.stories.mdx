import { ArgsTable, Title, Description } from '@storybook/addon-docs';
import { Meta } from '@storybook/addon-docs';
import DocsGenerator from './docs-generator';
import { DEFAULT_REDUX_ACTIONS_ACTIONS_PATH } from '../../../../../stories/constants/storybook';

<Meta
  title={`${DEFAULT_REDUX_ACTIONS_ACTIONS_PATH}submitTrixtaActionResponse`}
  parameters={{
    status: {
      type: 'beta', // 'beta' | 'stable' | 'deprecated'
    },
  }}
/>

<Title>submitTrixtaActionResponse</Title>
<Description>
  This is the main function to submit data to Trixta for a ```actionName``` and
  ```roleName```. The submit prop in action components and hooks make use of this
  function.
</Description>
<ArgsTable of={DocsGenerator} />
