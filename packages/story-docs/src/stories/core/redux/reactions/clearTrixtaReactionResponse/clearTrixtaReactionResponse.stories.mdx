import { ArgsTable, Title, Description } from '@storybook/addon-docs';
import { Meta } from '@storybook/addon-docs';
import DocsGenerator from './docs-generator';
import { DEFAULT_REDUX_ACTIONS_REACTIONS_PATH } from '../../../../constants/storybook';

<Meta
  title={`${DEFAULT_REDUX_ACTIONS_REACTIONS_PATH}clearTrixtaReactionResponse`}
  parameters={{
    status: {
      type: 'released', // 'beta' | 'stable' | 'deprecated'
    },
  }}
/>

<Title>clearTrixtaReactionResponse</Title>
<Description>
  This will clear the responses for the ```reactionName``` and ```roleName```. The responses
  are what is rendered in reaction components and hooks.
</Description>
<ArgsTable of={DocsGenerator} />
