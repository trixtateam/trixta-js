/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable @typescript-eslint/explicit-module-boundary-types */
import { UseRespondToReactionResponseProps } from '@trixtateam/trixta-js-core';

/**
 * This hook will respond to the latest reaction received from **Trixta**, for the given **roleName** and **reactionName**. You can respond by using the
 * returned **respondToReaction** callBack function
 * @param parameters
 * @returns
 */
export function DocsGenerator(parameters: UseRespondToReactionResponseProps) {
  return null;
}

export default DocsGenerator;
