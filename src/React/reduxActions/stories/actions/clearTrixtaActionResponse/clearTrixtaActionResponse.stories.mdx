import { ArgsTable, Title, Description } from '@storybook/addon-docs';
import { Meta } from '@storybook/addon-docs';
import DocsGenerator from './docs-generator';
import { DEFAULT_REDUX_ACTIONS_ACTIONS_PATH } from '../../../../../stories/constants/storybook';

<Meta
  title={`${DEFAULT_REDUX_ACTIONS_ACTIONS_PATH}clearTrixtaActionResponse`}
  parameters={{
    status: {
      type: 'released', // 'beta' | 'stable' | 'deprecated'
    },
  }}
/>

<Title>clearTrixtaActionResponse</Title>
<Description>
  This will clear the responses for the ```actionName``` and ```roleName```. The responses
  are what is rendered in action components and hooks.
</Description>
<ArgsTable of={DocsGenerator} />
