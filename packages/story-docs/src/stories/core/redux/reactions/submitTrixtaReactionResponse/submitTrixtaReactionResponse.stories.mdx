import { ArgsTable, Title, Description } from '@storybook/addon-docs';
import { Meta } from '@storybook/addon-docs';
import DocsGenerator from './docs-generator';
import { DEFAULT_REDUX_ACTIONS_REACTIONS_PATH } from '../../../../constants/storybook';

<Meta
  title={`${DEFAULT_REDUX_ACTIONS_REACTIONS_PATH}submitTrixtaReactionResponse`}
  parameters={{
    status: {
      type: 'beta', // 'beta' | 'stable' | 'deprecated'
    },
  }}
/>

<Title>submitTrixtaReactionResponse</Title>
<Description>
  This is the main function to submit data to Trixta for a ```reactionName``` and
  ```roleName```. The submit prop in reaction components and hooks make use of this
  function.
</Description>
<ArgsTable of={DocsGenerator} />
