import { ArgsTable, Title, Description } from '@storybook/addon-docs';
import { Meta } from '@storybook/addon-docs';
import DocsGenerator from './docs-generator';
import { DEFAULT_REDUX_ACTIONS_REACTIONS_PATH } from '../../../../../stories/constants/storybook';

<Meta
  title={`${DEFAULT_REDUX_ACTIONS_REACTIONS_PATH}clearTrixtaReactionRequestStatus`}
  parameters={{
    status: {
      type: 'released', // 'beta' | 'stable' | 'deprecated'
    },
  }}
/>

<Title>clearTrixtaReactionRequestStatus</Title>
<Description>
  This will clear the request status for the ```reactionName``` and ```roleName```. The
  request status determines isInProgress for the reaction components and hooks.
</Description>
<ArgsTable of={DocsGenerator} />
